using namespace std;

int main(){
  vector<int> v(n);
  // 第3引数は、v[0]の値
  iota(v.begin(), v.end(), 1);  

  do{
      for(int i=0; i<n; i++){
        
      }
    }while(next_permutation(v.begin(),v.end()));
  return 0;
}